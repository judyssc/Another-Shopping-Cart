
@{
    ViewBag.Title = "ViewCart";
}
@using S_cart.Models;
@{
    int userid = (int)ViewData["uid"];
    int cart_count = (int)ViewData["count"];
}


<div>
    
</div>

<h2>Your Cart</h2>
<table align="center" cellpadding="15" style="text-align:center">
    <tr align="center" style="text-align:center; font-size:large">
        <th>
            @*Sl_no*@
        </th>
        <th>
            @*Image*@
        </th>
        <th>
            @*<span style="font-size:16px; color:cadetblue">Quantity</span>*@
        </th>
        <th>
            @*<span style="font-size:16px; color:cadetblue">Total Price</span>*@
        </th>
    </tr>

    @foreach (Cart item in ViewBag.list)
    {
        int total = @item.quantity * @item.unit_price;
    <tr>
        <td style="color:white" >@item.product_id</td>
        <td><img src="@item.image_url" height="100" width="100" /></td>
        <td>Quantity: @item.quantity</td>
        <td>Total: $@total</td>
        <td>
            <input type="button" value="REMOVE" onclick="location.href='@Url.Action("rfc", "ViewCart",new { p_id = @item.product_id,u_id = userid })'" />
        </td>
    </tr>
      
    }
    
    <tr>
        <td colspan="5" align="right">
            @*<a href="@Url.Action("Search", "Gallery", new { uid = userid })">
                <button>Go back to edit Quantity</button>
            </a>*@
            <a href="@Url.Action("Search", "Gallery", new { uid = userid })">
                <button>CONTINUE SHOPPING</button>
            </a>
            <input type="button" value="CHECK OUT" />
        </td>
        @*<td>
            <input type="button" value="Check out" />
        </td>*@
    </tr>
</table>

@*<div>
    <a href="@Url.Action("Search", "Gallery", new { uid = userid })">
        <button>Continue Shopping</button>
    </a>
</div>*@

